# reversejs
A simple javascript function which reverses and returns the input given to it.  (Works with emojis!) 

# To use
Call the function reverse() and put input you would like to reverse inside the parenthesis
